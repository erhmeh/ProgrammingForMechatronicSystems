class Rectangle {
public:
bool setWidthHeight (double i,double j);
bool setWidthHeight (double k);
double getWidth();
double getHeight();
double area ();
double perimeter();
private:
double width_, height_;
};
